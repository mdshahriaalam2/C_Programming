#include <stdio.h>

int main()
{
    /*
    // Get odd number of n

    for (int i = 5; i <= 50; i++)
    {
        if (i % 2 != 0)
        {
            printf("%d\n", i);
        }
    }

    // Factorial Number


    int n, fac = 1;
    printf("Enter a number: ");
    scanf("%d", &n);

    for (int i = 1; i <= n; i++)
    {
        fac *= i;
    }
    printf("%d\n", fac);



    // Reverse a table

    int n;
    printf("Enter a number: ");
    scanf("%d", &n);
    for (int i = 10; i >= 1; i--)
    {
        printf("%d X %d = %d\n", n, i, n * i);
    }

    */
    return 0;
}
